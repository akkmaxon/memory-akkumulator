<div id="welcome-container">
  <h2><%= t('.forgot') %></h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= devise_error_messages! %>

    <div class="field">
      <%= f.label :email, t('.email') %><br />
      <%= f.email_field :email, placeholder: 'Sorry, not ready yet', readonly: true, class: "form-control" %>
    </div>

    <div class="actions">
      <%= f.submit t('.confirm'), class: "btn btn-primary", disabled: true %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
